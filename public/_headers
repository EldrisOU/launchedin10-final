# .pages.dev blocking handled by CF Bulk Redirects (account-level)
# _headers file cannot distinguish between .pages.dev and custom domain
# DO NOT add X-Robots-Tag: noindex here — it would noindex the real site too
